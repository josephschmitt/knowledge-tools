# Index

Navigation map for `library/`, maintained by the librarian. See `CLAUDE.md`. One line
per note, summarizing it, grouped under category headings. The categories below are
a starting scaffold — rename, add, and reorganize them as the corpus grows.

Once `notebook/` is populated, the compile keeps a `## Notebook` pointer block above the
library-nav sections, linking to the notebook home `[[notebook/index]]` (notebook entries are
navigated there, not listed individually here).

## Decisions

## Reference

## Concepts & Hubs
