# Log

Append-only, chronological record of what has happened to the vault — compiles,
queries that filed new knowledge, and health checks. One line per event, newest at
the bottom, each prefixed with an ISO date. Never rewrite or delete past entries.
