{
  "name": "knowledge-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Knowledge vault service: exposes the vault over both a Streamable HTTP MCP endpoint (/mcp) and a REST API (/api/v1). Performs no auth itself by default — deploy it behind an authenticating reverse proxy, or enable built-in token validation.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "node --watch --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^5.0.0",
    "jose": "^6.2.3",
    "pino": "^10.3.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
