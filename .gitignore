# Nightly compile run logs (raw trail of inbox captures lives in inbox/archive/)
outputs/compile-logs/

# MCP server build artifacts / secrets
mcp/node_modules/
mcp/dist/
mcp/.env
mcp/data/

# Per-skill release zips (built in CI by package-skills.yml)
dist/

# Compile coordination state (sentinel, status, lock — runtime only)
inbox/.compile/
