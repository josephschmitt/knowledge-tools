# Local env files (never commit; *.env.example is the committed template)
.env

# knowledge-service build artifacts / secrets
service/node_modules/
service/dist/
service/data/

# Per-skill release zips (built in CI by package-skills.yml)
dist/

# Local git worktrees (e.g. for parallel branch work)
.claude/worktrees/
